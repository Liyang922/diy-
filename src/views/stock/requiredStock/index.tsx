import { connect } from "react-redux";

function RequiredStock(props : any) {
    return (
        <div>RequiredStock</div>
    )
}

// export default connect()(RequiredStock);
export default RequiredStock;